---
import { euro, type Product } from '../content/site';
interface Props { product: Product }
const { product } = Astro.props;
---
<article class="card reveal">
  <a href={`/produkt/${product.slug}/`} class="media">
    <img src={product.image} alt={product.name} loading="lazy" width="800" height="800" />
  </a>
  <div class="body">
    <h3 class="serif"><a href={`/produkt/${product.slug}/`}>{product.name}</a></h3>
    <p class="dim short">{product.short}</p>
    <div class="row">
      <span class="gold price">{euro(product.price)}</span>
      <button type="button" class="btn add" data-add-to-cart data-slug={product.slug} data-name={product.name} data-price={product.price}>
        In den Warenkorb
      </button>
    </div>
  </div>
</article>

<style>
  .card { display: flex; flex-direction: column; }
  /* Product photos always on cream (design rule — Stitch fix 5) */
  .media { background: var(--paper); aspect-ratio: 1; overflow: hidden; }
  .media img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.5s ease; }
  .card:hover .media img { transform: scale(1.04); }
  .body { padding: 18px 0 8px; display: flex; flex-direction: column; gap: 8px; flex: 1; }
  h3 { font-size: 1.25rem; }
  .short { font-size: 0.88rem; flex: 1; }
  .row { display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-top: 6px; }
  .price { font-size: 1.05rem; letter-spacing: 0.04em; }
  .add { padding: 0.6rem 1.1rem; font-size: 0.7rem; }
</style>
